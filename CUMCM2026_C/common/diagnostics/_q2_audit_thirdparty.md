# 问题二结果文件第三方独立复核（t3 / 复核者：建模手）

> 复核对象：`CUMCM2026_C/Q2/outputs/result2.xlsx`、`Q2_diagnostics.json`、`Q2_analysis.json`、
> `Q2_timeseries.csv`。
> 复核者身份：非 `result2.xlsx` 的作者，故组织意义上是第三方。
> 本文件是**属于我的复核记录**；编程手的自审产物 `Q2_audit.py` / `_q2_audit.txt` /
> `_q2_audit.json` 保持原样，我的产物统一用 `_q2_audit_thirdparty*` 前缀，两套并存。

## 零、结论

**verdict = pass**（就主口径 `result2.xlsx` 的数值正确性而言）。

> **2026-09-12 终版复检**：在 t10 落地后的最终产物上重跑本复核脚本，结果为
> **48 项检查全部 passed、fatal 0、verdict = pass**——此前唯一的结构偏离项 4c
> 已随“说明行改回 A1 批注”而消失。多处调整的收口证据见 §11.5。

## 一、独立路径（不用被复核方的代码）

| 项 | 做法 |
|---|---|
| 入口 | `_q2_audit_independent.py`（我的脚本），**零 import 项目代码**（仅 csv / json / math / re / numpy / openpyxl / scipy） |
| 附件解析 | openpyxl 自行读 `附件1.xlsx` / `附件2.xlsx`，自行做日期宽松解析与量纲折算 `kWh = kW × 10/60` |
| 时段标签 | 自行实现 `interval_label(t)`，不复用 `io_attachments` |
| 数值复核 | 自建 577 变量线性规划（`[S(145) \| x(144) \| y(144) \| g(144)]`），**独立重解全部 334 天**并与结果文件逐日对账 |
| 覆盖度 | 全 334 × 144 = 48 096 个时段逐点计算，无抽查 |
| 与编程手脚本的关系 | **独立运行**其 `Q2_audit.py` 确认可复跑（不引用其结论作为我的证据） |

核对全部落盘于 `_q2_audit_thirdparty.txt`（含『复核算式』专节）与 `_q2_audit_thirdparty.json`
（48 条 checks + caveats C0–C5 + key_numbers）。

## 二、队长指定抽查项（全部通过）

从 `result2.xlsx` 与 `Q2_timeseries.csv` 自行复算，与队长给出的期望值比对：

| 抽查项 | 我的复算值 | 期望值 | 偏差 | 判定 |
|---|---|---|---|---|
| 全年计划购电量（`全天购电量` 列求和） | 20 218 838.180252 | 20 218 838.180253 | 1.0e-06 kWh | PASS |
| 全年计划购电量（144 列全量求和，独立算式） | 20 218 838.180329 | 20 218 838.180253 | 7.6e-05 kWh | PASS |
| 全年购电费（`全天购电费` 列求和） | 12 245 046.915277999 | 12 245 046.915278 | 1.9e-09 元 | PASS |
| 2025-03-20 全天购电量 | 66 322.044939 | 66 322.04493868313 | 3.2e-07 kWh | PASS |
| 2025-06-21 全天购电量 | 32 667.359783 | 32 667.359783333337 | 3.3e-07 kWh | PASS |
| 2025-09-23 全天购电量 | 66 265.421309 | 66 265.42130905349 | 5.3e-08 kWh | PASS |
| 2025-12-21 全天购电量 | 93 770.214940 | 93 770.21493971194 | 2.9e-07 kWh | PASS |
| 附件 1 典型日复算费用（自建 LP） | 35 126.948589289634 | 35 126.948589290 | 3.6e-10 元 | PASS |
| 明细 CSV 的 `计划购电量` 合计（与宽表互相独立） | 20 218 838.180329 | 20 218 838.180253 | 7.6e-05 kWh | PASS |

> 两个“独立算式”给出 20 218 838.180252 与 …180329，差 7.6e-05 kWh（相对 3.8e-12）。
> 成因是宽表单元格按 6 位小数落盘（334 × 144 = 48 096 个单元格各舍入 ±5e-7 ⇒ 最坏
> 累计 0.024 kWh，实测 7.6e-05 远低于该上界）。**不构成差异**。

**更强的证认**：自建 LP 独立重解 334 天的全年最优费用为 **12 245 046.915277638 元**，
与结果文件逐日汇总 **12 245 046.915278 元** 相差 **3.6e-07 元**；逐日费用最大偏差 < 1e-6 元。
这与编程手自审脚本独立给出的 `independent_cost_yuan = 12245046.915277638` **完全一致**，
两套互不共享代码的实现互相印证。

## 三、独立运行编程手自审脚本的结果：**exit 1（不是 exit 0）**

| 项 | 结果 |
|---|---|
| 命令 | `.venv\Scripts\python.exe CUMCM2026_C\common\diagnostics\Q2_audit.py` |
| exit code | **1** |
| 汇总 | `n_checks=20, n_passed=19, n_failed=1, verdict="needs_revision"` |
| 失败项 | `(6) 紧急购电量表：3 列表头 + '起-止' 格式 + 主口径 0 条明细` — “表头 ['日期','购电时间段','购电量']；**数据行 1（主口径下预期 0）**；时间段格式合规=True” |
| import 独立性 | 脚本 import 仅 `json / sys / datetime / pathlib / numpy / openpyxl`，以及函数内的 `scipy.optimize.linprog`，**不 import 任何被复核模块**，独立性成立 |

队长交代的是“编程手的复核已完成并落盘（exit 0）”。我独立复跑得到的是 **exit 1**：
`紧急购电量` 表被加上了 t6 收口清单要求的说明行，而它自己的第 6 项检查仍按“0 条明细”判，
于是同一套产物在**脚本未被修改**（`Q2_audit.py` mtime 仍为其 3:27:43 原版）的情况下由通过转为失败。

这与我方检查 4c 的结论同源，可互相印证：

- **我方**：4c FAIL，理由是说明行占用『日期』列，严格校验器会判非法日期；
- **编程手方**：check 6 FAIL，理由是数据行数 1 ≠ 0。

两条独立判据指向同一个结构偏离。**建议**：提交版把说明移回 **A1 单元格批注**（编程手最初的
做法），或移到表外/诊断 JSON；届时两套校验器都会恢复通过，人工可读性也不损失。

## 四、U1（量级异常）：**独立复核后关闭，判定不成立**

我自行读取附件原始数据算得：

| 量 | 我的实测 | 队长断言 | 一致 |
|---|---|---|---|
| 附件 2 光伏实际功率峰值 | **10 216.20 kW** | 10 216.20 kW | 是 |
| 附件 2 光伏 99.9 分位 | **9 739.22 kW** | 9 739 kW | 是 |
| 附件 2 逐日最大值的日中位 | **8 161.05 kW** | 8 161 kW | 是 |
| 附件 2 逐日最大值的日最小 | 5 426.81 kW | — | — |
| 附件 2 形状 | 365 × 144 | — | — |
| 附件 3 光伏预报峰值 | **9 995.8875 kW** | 9 995.89 kW | 是 |
| 附件 3 光伏预报 99.9 分位 | 9 965.48 kW | — | — |
| 附件 3 形状 | 1 460 × 24 | — | 是 |

附件 2 与附件 3 的光伏峰值同为 10 MW 量级，同尺度。编程手所报“实际约 1400 kW”与附件数据
不符。**U1 关闭**。（附注：附件 2 的 *小区负载* 在 3 300–6 000 kW 量级，若把负载误当光伏，
易得千 kW 量级的错觉；这可能是该误判的来源。）

## 五、U4（对照口径绝对量级不可独立复现）：**保留为未决，本轮不下结论**

队长要求复现“同一天两个实现给出相同的 (q.sum, x.sum, y.sum, 目标值) 但不同的紧急购电量”。
**我本轮未做该项数值复现，理由是它依赖正在被 t6 重写的预报构造链路**（`forecast_from_release`）：
在 t6 落地前复现，得到的只是即将失效的中间态，且可能给论文一个错误确认。

现存证据（取自编程手自审 JSON 的 `control_b2ii`，仅作记录，不作为我的结论）：

| 量 | 独立重算 | JSON 记录 | 比值 |
|---|---|---|---|
| 对照口径紧急购电量（全年） | 19 054 624.739 kWh | 1 262 153.804 kWh | **约 15.1 倍** |
| 对照口径总费用（全年） | 89 465 832.714 元 | 17 938 582.489 元 | 约 5.0 倍 |

**与队长的处置一致**：对照口径的**绝对量级不可引用**，论文侧改用定性表述。
**t7 落地后我将按以下固定配方复现并给出裁决**（T = 冻结后的 `Q2_analysis.json`）：

1. 取 T 中参考变体（仅光伏不可预知）的 `daily.plan_total_kwh` / `actual_total_kwh` /
   `emergency_total_kwh`，选一个紧急购电非零且量级差异最大的日期 d*；
2. 用我自建的 577 变量 LP、以附件 3 第 d* 日 0:00 发布的预报为入口重解**计划阶段**，
   与 `plan_total_kwh[d*]` 比对 (q.sum, x.sum, y.sum, 目标值)；
3. 以计划调度为数据、代入第 d* 日**实际**负载与光伏，按锁定执行语义算 a、e，
   与 `actual_total_kwh[d*]` / `emergency_total_kwh[d*]` 比对；
4. 若第 2 步吻合而第 3 步的 e 不吻合 ⇒ 队长要的那条证据成立（分歧只在执行阶段语义，
   不在计划阶段），否则给出反例并说明分歧位置。

## 六、U2 / U3

- **U2（验证的是实现忠实而非模型唯一正确）**：同意保留为 limitation。本复核能证明的是
  “结果文件与所声明的数学模型一致、且该模型被两套独立实现复现”，不能证明该模型是题面
  的唯一正确解读。此项已写入 `_q2_audit_thirdparty.json` 的 caveats。
- **U3（组织独立性）**：由我从非作者视角执行本复核即告解决。补充：我不仅复跑了编程手的
  脚本，还另写了一套零共享代码的实现，两套实现互相印证（见第二节末）。

## 七、我方其余存疑项（不影响主口径数值）

| id | 级别 | 内容 |
|---|---|---|
| C0 | low | `紧急购电量` 说明行占用『日期』列（= 第三节的同一件事） |
| C1 | low | 契约 §7 第 5/6/8/10 项的逐时段复核只能基于 `Q2_timeseries.csv`，与 `result2.xlsx` 同源；已用“CSV → 区块汇总 vs xlsx”与“独立 LP vs 宽表”双交叉约束，但仍非完全独立数据源 |
| C2 | low | 方案 A 表头与附件 5 模板逐字符不同（契约 §5.5 决议 2 已裁定，属有意偏离） |
| C3 | medium | B2(ii) 的 `forecast_from_release` 丢 k=24、`hourly[0]` 恒 0 ⇒ t=1..6 置 0，与 R-Q2-5 公式不一致（t6 正在修） |
| C4 | medium | 两个调度变体与契约原表述相反；`Q2_analysis.json` 生成于订正之前 |
| C5 | low | 无法验证『零目标可行性预解』与『对抗性紧急购电反查』是否真的执行过，只能读 JSON 自述 |

## 八、复核局限（不得隐瞒）

1. 本次复核冻结的产物版本：`result2.xlsx` mtime 2026-09-12T03:30:24。
   **t6 仍在改 `Q2_analysis.py`**，故 C3 / C4 / U4 涉及的对照口径数值需在 t6 落地后重做。
2. 容差：逐时段残差按 **5e-6** 判（而非契约 §7 写的 1e-6），依据是 `Q2_timeseries.csv`
   以 `%.6f` 落盘、递推式系数绝对值之和 ≈ 3.011 ⇒ 最坏舍入残差 ≈ 1.5e-6。实测最大
   1.522e-06 kWh，落在该上界内。**建议契约 §7 第 5 条同步放宽或注明该舍入口径。**
3. 独立解与结果文件的**逐时段**购电量每天相差最多 333.33 kWh，属**多重最优**（逐日费用
   吻合到 1e-6 元、逐日购电总量吻合到 1e-3 kWh、逐日差额之和为 0，附件 1 的 144 个电价中
   仅 137 个互异，存在平局空间）。故比较取“费用与总量”为判据，不取逐时段相等。
4. 本复核未审阅 `Q2_solve_plan.py` 与 `Q2_analysis.py` 的实现正确性本身（超出 t3 范围），
   只审其**产物**与契约的一致性。

## 九、产物清单

| 文件 | 内容 |
|---|---|
| `_q2_audit_independent.py` | 我的自包含复核脚本（零 import 项目代码）；产物前缀为 `_q2_audit_thirdparty` |
| `_q2_audit_thirdparty_probe.py` | 队长指定抽查 9 项 + U1 量级复核的探针脚本 |
| `_q2_audit_thirdparty.txt` | 复核过程：『复核算式』专节 + 48 项 PASS/FAIL 与实测值、位置 |
| `_q2_audit_thirdparty.json` | 结构化结论：48 条 checks、key_numbers、tolerance_note、caveats C0–C5、verdict |
| `_q2_audit_thirdparty_probe.json` | 队长指定抽查 9 项 + U1 量级复核的实测值 |
| `_q2_audit_thirdparty.md` | 本文件 |
| `Q2_audit.py` / `_q2_audit.txt` / `_q2_audit.json` | 编程手的自审产物，**保持原样**，由我独立复跑再生 |

## 十一、C3/C4 二次复核（t6 落地后补做，2026-09-12）

队长通报 t6 已修好 C3/C4。我按磁盘现状独立复核，结论是 **C3 只修好了一半**。

### 11.1 已确认修好的部分

`Q2_analysis.py::forecast_from_release` 第 202 行改为 `absolute_block = release_hour + k - 1`，
即第 $k$ 列对应**发布时刻之后第 $k$ 个小时块**。取 $r=0$、`release_day = target_day` 时
$k=1..24$ 映射到小时块 $0..23$，全部落在当天，`hourly[0..23]` 均被赋值 ⇒ `uncovered = []`，
**当天 144 个时段不再有零填充**。在用口径 `forecast_from_release(att3, day, 0, day)`
（第 345 行）与 R-Q2-5 的公式一致了。

实测：`Q2_analysis.json` 的 `forecast_errors.pv_forecast_a_vs_actual`
= MAE **371.2207801792249** / RMSE **648.3065024215832** / 偏差 **−29.6762228147871** /
P90 **1185.3359**，与契约参考值一致。

### 11.2 未修好的部分（同一文件内两套映射并存）

**第 409–415 行的横向对比仍用旧映射** `absolute = release_hour + k`（注意是 `+k`，
不是 `+k−1`）；取 $r=0$ 时只有 $k=1..23$ 落在当天，$k=24$ 被丢弃，`hourly[0]` 恒 0
⇒ 该分支仍把 $t=1..6$ 置 0。

于是**同一个 JSON 里对同一个发布时刻给出两个 MAE**：

| JSON 字段 | 值（kW） | 来源分支 | 映射 |
|---|---|---|---|
| `forecast_errors.pv_forecast_a_vs_actual.mae_kw` | **371.2208** | 第 190–209 行（在用的预报 A） | `+k−1`（正确） |
| `forecast_errors.by_release_hour["当日 00:00 发布"].mae_kw` | **448.3124** | 第 409–415 行（横向对比） | `+k`（旧） |
| `by_release_hour["当日 06:00 发布"].mae_kw` | 430.1181 | 同上 | `+k` |
| `by_release_hour["当日 12:00 发布"].mae_kw` | 1722.4903 | 同上 | `+k` |
| `by_release_hour["前一日 18:00 发布"].mae_kw` | 496.3898 | 同上 | `+k` |

**这条发现直接解释了队长与编程手此前 371.22 vs 448.31 的分歧**：它不是“分母口径”
之争，而是**同一份产物里两个代码分支用了两套相差一个小时块的映射**。

后果有三：(1) 论文若从 `by_release_hour` 取数，会拿到与在用口径不自洽的值；
(2) 该横向对比表内部就不自洽（其余各行同样建立在旧映射上），
“0:00 发布不如 6:00 发布准”这一结论是**拿新映射的行与旧映射的行比**，不可用；
(3) `by_release_hour` 第 418 行自报 `covered_hours_per_day = 24 - release_hour`
（$r=0$ 时 = 24），与它自己实际只覆盖 23 个块的实现互相矛盾。

### 11.3 其余与映射不一致的陈旧描述（均为文本字段，不影响数值）

| 位置 | 现值 | 修正后应为 |
|---|---|---|
| `Q2_analysis.py:364` | `"covered_hours_per_day": 23` | 24（`uncovered_a` 现为空列表） |
| `Q2_analysis.py:430` | “0:00 那一小时的 6 个时段无对应预报列，按 0 计入” | 已不再成立 |
| `Q2_analysis.py:434` | “仅统计被预报覆盖的时段 t = 7..144（138/144 个时段、24/24 个小时块）” | 自相矛盾且已失效；修好后无“未覆盖时段” |
| `Q2_analysis.json:forecast_definitions["预报A（主用）"]` | “覆盖当天 1:00–24:00” | 覆盖当天 0:00–24:00 的 24 个小时块 |

### 11.4 我方结论

- **C3 部分关闭**：在用预报口径已修好（MAE 371.22，144 时段无零填充）；
  **横向对比分支与四处文本字段仍未同步**，需在 t6 收口时一并处理，
  否则 t4/t9 有取到不自洽数值的风险。
- **C4**：`Q2_analysis.json` 的生成时间晚于脚本，且两个调度变体的相对大小
  与契约订正后的表述一致（承诺调度 1 262 154 kWh < 日内再调度 3 783 738 kWh，
  按“可复现性”而非“保守/乐观”定性），此项可关闭。
- 我 **§八.2 的写法据此确定**：写“第 $d$ 日 0:00 发布那条的 24 列覆盖当天
  0:00–24:00 的全部 24 个小时块、144 个时段无缺口”，不写覆盖率分数、不写
  `uncovered_hours`（修好后该列表为空）。


### 11.5 终版复检：C0 / C3 / C4 全部关闭，48 项全通过

在 t10 落地后的最终产物上重跑本复核脚本，结果 **48 项检查全部 passed、fatal 0、verdict = pass**
（此前唯一的结构偏离项 4c 已随“说明行改回 A1 批注”而消失）。逐项复检证据：

| 项 | 终版实测 |
|---|---|
| `pv_forecast_a_vs_actual.mae_kw` | 371.2207801792249 |
| `by_release_hour["当日 00:00 发布"].mae_kw` | 371.2207801792249（与上一行**逐位相等**，双映射已消除） |
| `covered_hours_per_day` 键 | 已从 `forecast_definitions` 删除 |
| `mae_kw_covered_only` | 在 JSON 中出现 **0 次** |
| `紧急购电量` 表 | **max_row = 1**（仅表头），A1 批注在位，第 2 行为空 |
| 独立复跑 `Q2_audit.py` | **exit 0 / n_checks=20 / passed=20 / failed=0 / verdict=pass** |

我方脚本的终版关键数（可用作交叉印证的定值）：

| 量 | 值 |
|---|---|
| 结果文件全年费用（逐日汇总） | 12 245 046.915277999 元 |
| 自建 LP 独立重解全年费用 | 12 245 046.91527763 元（差 3.7e-07 元） |
| 附件 1 特例独立重解费用 | 35 126.948589289634 元 |
| 逐时段 SOC 递推最大残差 | 1.522222e-06 kWh（落在 CSV 舍入上界内） |
| 功率平衡最大残差 | 1.000000e-06 kWh |
| 费用复算最大残差 | 6.599999e-06 元 |
| 全年 Σy/Σx | 0.8100000002233259 |

因此 §11.2 的“同一 JSON 两个 MAE”与 §11.3 的四处陈旧字段均已消除；C3 由“部分关闭”
转为**关闭**，C4 亦**关闭**（§11.4）。`_q2_audit_thirdparty.json` 的 caveats 已同步更新为
`C0/C3/C4 = closed`，C1/C2/C5 保留（分别属数据源同源、方案 A 有意偏离、过程性检查不可复现）。

## 十二、更正声明（流程透明度）
在我完成 t3 时，尚未收到“不要改他的产物”的指示，我的脚本当时与编程手脚本**同名落盘**，
因此 `_q2_audit.txt` / `_q2_audit.json` 一度被我的版本覆盖。收到指示后我已：
（1）把我方产物改名为 `_q2_audit_thirdparty.*`；
（2）**独立复跑 `Q2_audit.py` 以再生成其原产物**（现 `_q2_audit.json` 10 625 B、
`_q2_audit.txt` 13 844 B，与其原始 10 615 B / 13 834 B 同一量级，差异来自其内部时间戳）。
两套产物现已并存，互不覆盖。特此记录。

另：`common/tools/q2_audit_independent.py`（为迁就旧 verify 字符串而加的 9 行转发封装）
已按队长裁定**删除**，复核脚本与产物统一留在 `common/diagnostics/`。删除后 `tools/` 下
已无任何 q2 复核相关文件。
