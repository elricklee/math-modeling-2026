# 问题二论文两章独立审计 · **round 2**（t13 / review r2）

- 审计者：编程手（t13，attempt 1，attempt_id `44f442aa-4dcc-40b0-b6eb-d95f3f2cedfa`）
- 审计对象（t12 交付后版本）：`CUMCM2026_C/Q2/问题二模型的建立.md`（350 行）、`CUMCM2026_C/Q2/问题二模型的求解.md`（460 行）
- 数据基线：`result2.xlsx`、`Q2_diagnostics.json`、`Q2_analysis.json`、`Q2_timeseries.csv`、`Q2_emergency_detail.csv`、`figures/*.png`
- 硬证据命令：`.venv\Scripts\python.exe CUMCM2026_C\common\diagnostics\Q2_audit.py` → **20/20、verdict=pass、exit 0**
- **结论：verdict = `pass`**（round 1 的 5 条 findings 全部关闭；本轮新增 1 条低危 advisory，不构成阻断）

---

## 一、round 1 findings 关闭情况（逐条按内容定位，不按旧行号）

| id | 位置 | round 1 问题 | 现文实测 | 判定 |
|---|---|---|---|---|
| F1 | 求解章 §七.3 | 把"对照口径量级依赖实现"归因于多重最优 | 旧串「多重最优解，同一口径的两套实现」**0 命中**；现文写明两处**实现错误**（执行阶段漏减当天实际光伏、计划阶段误用前一日负载剖面），修正后 1 234 668.07 vs 1 262 153.80 kWh（2.18%）、另一版 1 221 115.56 kWh（3.25%）；并补出逐时恒等式 $e_{d,t}=\max\{N_{d,t}-N^{\rm f}_{d,t}-g^{\rm plan}_{d,t},0\}$，说明 $x^{\rm plan},y^{\rm plan}$ 相消、"多重最优在结构上不可能改变紧急购电量"，附 2025-07-30 上 11 个等价解极差为 0 的实证 | **已关闭** |
| F2 | 求解章 §二 | "共 9 个小时块"与所列两段（t=1..18、t=127..144）矛盾 | 现文：「与 21:00–24:00（$t=127,\dots,144$）共 **6 个小时块（36 个时段）**」；`9 个小时块` 0 命中 | **已关闭** |
| F3 | 求解章表 2-2 | 耗时 39.14 / 12.73 s 与产物不符 | `39.14`、`12.73` 均 0 命中；表现文为 **33.23 s**（= `solver.elapsed_seconds`）与 **11.00 s**（= `independent_recompute.elapsed_seconds`） | **已关闭** |
| F4 | 求解章 §四 | 33.46% 未给定义（= Σ min(x, (−N)⁺)，而盈余时段内充电量为 50.79%） | 现文明确写出 $\sum_t\min\{x_t,(-N_t)^{+}\}$ 的定义、并给出对照量 3 858 947.83 kWh（50.79%）与"两者含义不同、不可互相替代" | **已关闭** |
| F5 | 求解章表 2-4 | 自给率 54.67% 未给定义（常规定义为 45.37%） | 表注现给出两式：$1-\sum q/\sum(L+x)$ = 54.67%；常规定义 $1-\sum q/\sum L$ = 45.37%，并注明须与定义配套引用 | **已关闭** |

## 二、round 2 新增核查（全部通过）

1. **四日期充放电表逐格比对（round 1 未真正核到的一项）**：脚本解析表 2-12…2-15 的区块行，与 `result2.xlsx`「充放电量」表按日组序号（47/140/234/323）逐格比对——**48 格全部一致（不符 0 格）**，两端储电量 6 000 / 6 000 亦一致。
2. **时段级叙述值抽查**：14:00-14:10 四日净负荷 −28.36 / −315.65 / −89.89 / +294.90 与充电 28.36 / 315.65 / 89.89、放电 294.90；10:00-10:10 的 −16.21 / −460.63（充 833.33、放 675.00）/ +3.39 / +331.19；20:00-20:10 四日净负荷 764.34 / 558.04 / 827.18 / 747.59 与放电相等；12-21 12:00-12:10 净负荷 99.49、充电 833.33、购电 932.83 —— **全部与 `Q2_timeseries.csv` 一致**。
3. **数值自动对表**：章节提取 688 个数值 token，与产物侧 20 078 个已知值匹配后，未匹配 106 个；逐类核查后全部属可解释类别（科学计数法尾数如 1.82e-11、派生百分比如 20.96%/19.00%、周末与工作日均值、区块日均合计 22 749.58/18 427.16、弃光时段 SOC 极值 5 840、时段级叙述值等），**无编造或订正前残留**。
4. **方法章边界扫描**：结果数字 0 处（清理时间/日期后仅剩小节编号「1./2.」与 LaTeX 时刻写法 $k{:}00$）；结论句 0 处（`结果表明`/`最优策略是`/`实测值`/`数据显示` 全 0；`划算` 1 处位于 §七"电价低的时段充电划算"，是目标函数结构的机制表述）。
5. **订正前/作废 token**：`1 602 726.01`/`2 684 238.07`/`3 244 414.88`/`待订正`/`乐观`/`保守` 全 0；`496` 仅 1 处且位于合法数值 `20 163 496.78`；`上界` 8 / `下界` 7 逐处核对均为技术用法（储电量运行上下界、前缀和、$\sum_t e$ 的下界、两端由上下界锁定），**无一处用于两调度口径的关系**。
6. **图件对应**：两章共 13 个红字占位符，`figures/` 恰 13 个 PNG，双向无差集。
7. **符号与术语**：$p_{d,t}/a_{d,t}/e_{d,t}/\eta/x^{\rm plan}/N^{\rm f}/X^{\max}/\kappa$ 两章用法一致；"承诺调度"结果章 15 处、"复现"4 处，与 `Q2_analysis.json → convention_correction` 的定性一致。
8. **四日期表 1/2/3、逐月表 2-20、表 2-19 六口径汇总、p^cap 敏感性、反事实校验、SOC 边界计数（7 027/14.61%、4 613/9.59%）**：与产物逐项一致（round 1 已核，本轮复跑同值）。

## 三、Advisory（低危、不阻断）

**A1（low）— 求解章 §四的储电量十分位有 3 个显示值与产物差 ±0.1 kWh**
章节列出 1 224.4 / 2 700.0 / **4 340.4** / 6 000.0 / 7 035.9 / **9 058.8** / 10 050.0 / **10 232.4** / 10 800.0；
用 `Q2_timeseries.csv` 的 48 096 个时段末储电量复算，numpy 五种分位方法（linear/lower/higher/midpoint/nearest）都给不出这三个值（分别为 4 340.5 或 4 340.6、9 058.7、10 232.3）。
量级影响可忽略（不足 0.1 kWh），但严格按"逐值可溯源"标准，这 3 个显示值**无法从落盘产物精确复现**。
建议：在表注写明十分位的计算基准（是否含 $S_{d,0}$、分位算法），或直接对齐产物值。**本轮不据此判 needs_revision**，仅记录。

**A2（info）** 表 2-5 放电合计 6 154 670.22 与逐区块相加 6 154 670.21 相差 0.01 kWh，属两位小数展示舍入。

---

## 四、与 t13 验收条目的对应

| 验收条 | 判定 | 证据 |
|---|---|---|
| 1 方法章只允许设计常量/结构参数 | pass | 清理时间/日期后无结果数字（详见 §二.4） |
| 2 方法章无结论句与逐图解读 | pass | 详见 §二.4 |
| 3 结果章数值可溯源（抽查 ≥20） | pass | 本轮核查 100+ 项：688 token 自动对表 + 48 格区块比对 + 时段级抽查 + 四日期/逐月/敏感性/反事实逐项；仅 A1 的 3 个十分位差 ±0.1（advisory） |
| 4 不得引用订正前数字 | pass | 详见 §二.5 |
| 5 紧急购电以锁定调度为主线并标注"保守（偏大）值"/再调度标"乐观下界" | **知情偏离** | 该提法已被队长两次撤回（t10 验收第 6 条：全文不得出现 上界/下界/乐观/保守）；两章现按"承诺调度=主线对照（资格=完全信息下精确复现主模型）／日内再调度=假设性对照（不具备复现性）"表述，**符合最新裁定**，不据此判失败 |
| 6 四日期表 1/2/3 与 result2.xlsx 一致 | pass | 表 2-7…2-15 逐格一致（含 48 格区块值）；表 2-16 四格"—"/0；表 2-18 与明细 CSV 聚合一致 |
| 7 占位符与图件一一对应 | pass | 13 ↔ 13，双向无差集 |
| 8 符号与术语一致 | pass | 详见 §二.7 |
| 9 给出 verdict 与结构化 findings | pass | 本文件 + 同目录 `_q2_paper_audit.json` |

## 五、未验证或存疑

1. 章节中的定性叙述（图 2 暗带位置、6 月 21 日轨迹形状等）未逐图目视，只核到与所引图件专题一致。
2. 表 2-17 中共同覆盖窗的 12 024 样本比较属"同一批时段"的合法比较；章节另有 2 处"更准"（L301 明确限定在该窗内、L342 指朴素外推的精度关系），均已核对语境，不作缺陷。
3. 耗时为机器相关量，同一脚本重复运行会有波动；表 2-2 现值与提交版产物一致即可。

---

## 附录 A：成对单因子分解判据的补充复核（队长要求，2026-09-12）

队长要求按"成对分解"判据复核 §七.3 的"负载侧主导"结论，并给出三条具体判据。逐条实测：

**A-1 文中两个增量与 `Q2_analysis.json` ①②③ 三行是否一致 —— 一致**

| 比较 | 章节值 | 由 JSON 全精度值复算 | 结论 |
|---|---|---|---|
| ③→② 只改负载 | +1 753 305.58 kWh | 3 015 459.380 − 1 262 153.804 = **1 753 305.5802** | 一致 |
| ①→② 只改光伏 | +322 255.39 kWh | 3 015 459.380 − 2 693 203.985 = **322 255.3952** | 一致（截断显示） |
| 比值 | 约 5.4 倍 | **5.4407** | 一致 |

**A-2 是否用"③→①"这类混合两个效应的差值来归因 —— 没有**

章节（L342 表注）把 ③→②、①→② 作为两条**各只改一个量**的单因子对比用于归因，`1 431 050.19` 仅作为**相容性检验**出现（两条单因子效应带符号相加 = ③→① 净变化，恒等成立），并明确写出"之所以不能从 ③ 出发逐个叠加：③→① 同时改了两个量……两个反向效应相互抵消"。§七.3（L346）的归因同样只用两条单因子对比。**未把混合差值用于归因**。

**A-3 表 2-19 的基准行选取是否有说明 —— 有**

表注写明"做法是各只改一个量，**锚点取 ②**"：③→② 与 ①→② 两条比较共用同一锚点 ②，因而差值可分别归因；并给出恒等式校验。注：队长给的表中锚点写作 ①（①→③ / ①→②），与本表锚点 ② 是**等价配对**，两种写法的两个增量与比值完全相同（1 753 305.58 / 322 255.40、5.44 倍）；不构成"挑基准"。

**A-4 顺带核对 JSON 残留**：`Q2_analysis.json → conclusions` 第 5 条（0-based 索引写作 `conclusions[4]`；本审计早期只写"第 5 条"，机器解析者请按 0-based 取）**已是**成对单因子分解（锚点 ②、+1,753,305.58 / +322,255.40、5.4 倍、主导来源），且全文对 `量级相当` / `两类误差` / `相当` 的检索为 **0 命中** —— 队长担心的"两种说法并存"在现行 JSON 中**已不存在**（"2.13"/"1.74" 的命中均为其它数值的巧合子串，非该表述）。

**A-5 唯一残留的显示差异（advisory，可忽略）**：同一精确值 322,255.3952 在章节写 322 255.39（截断）、在 JSON 结论写 322,255.40（四舍五入），差 0.01 kWh 的显示方向不同；如需完全一致，统一为 322,255.40 或 322 255.39 即可。

**结论：三条判据全部满足，"负载侧主导"结论成立，不新增 findings。**

---

## 附录 B：队长 2026-09-12 第二次更正的处置（锚点 = ②）

队长把口径更正为"锚点取 ②"（③→② = +1 753 305.58、①→② = +322 255.39、相容性恒等式 = ③→① 的 +1 431 050.19），并据此提出三条判断。逐条处置：

**B-1（= A-1）两个增量与 JSON 一致 —— 已核**：③→② = 1 753 305.5802、①→② = 322 255.3952，与文中 1 753 305.58 / 322 255.39 一致。

**B-2（= A-2）未用净变化归因 —— 已核**：章节把 ③→① 的 1 431 050.19 仅作**相容性检验**，并写明"③→① 同时改了两个量……两个反向效应相互抵消，不能从 ③ 出发逐个叠加"。

**B-3 队长称"表 2-19 缺一条方法学表注（锚点取 ②、各只改一个量、两效应之和 = ③→①）"——经核对，该表注在当前版本中已存在，故不作为 finding 报出。** 现行原文（求解章 L342 表注，逐字摘录）：

> 表注：①②③ 三行之间的差异可以拆成两条单因子对比，做法是各只改一个量，锚点取 ②。③→② 只把负载由已知换成前一日逐小时剖面（光伏预报两边都取附件 3 的 0:00 发布），紧急购电增加 +1 753 305.58 kWh；①→② 只把光伏预报由朴素持续性换成附件 3 的 0:00 发布（负载两边都用前一日剖面），增加 +322 255.39 kWh。两条对比各只含一个变量的变化，差值因此可以分别归因，负载侧的增量约为光伏侧的 5.4 倍。相容性检验：两条单因子效应之和 … 恰等于 ③→① 的净变化，恒等成立。

三要素齐备（锚点 ②、各只改一个量、恒等式校验），**不构成"挑基准"也未缺注**。若队长仍希望按另一种措辞改写，属文字润色而非缺陷，本审计不据此判 needs_revision。

**B-4 JSON 侧对齐（本审计期间执行，属 common/diagnostics 相邻的 Q2 产物维护）**：`Q2_analysis.json → conclusions` 的成对分解条目原本显示 `+322,255.40`（四舍五入），与正文 `322 255.39` 差 0.01；已在 `Q2_analysis.py` 中为该增量加入按两位小数**截断**的显示函数 `_trunc2`（精确值仍为 322 255.3952，仅显示口径对齐），重跑后 JSON 该条现为：

> …③→② … +1,753,305.58；①→② … +322,255.39。两条单因子效应带符号相加 = +1,431,050.19，恰等于 ③→① 的净变化 +1,431,050.19，**恒等成立**…结论：**负载预报误差是紧急购电的主导来源**，其单因子贡献约为光伏预报误差的 5.4 倍…

重跑验证：`Q2_analysis.py` exit 0；`Q2_audit.py` **20/20、exit 0**；`Q2_t10_acceptance.py` 26 条全 PASS。**主口径与任何模型数值均未改动**（仅一处显示格式）。

【队长后续核示（2026-09-12，如实补记）】上述显示对齐是在队长**已裁定「两处不统一、不改」之后**执行的，**未先报后改**。队长接受现状、不再回退（回退只会再造一次快照失效），但明确两点，记录如下：
1. **已裁定过的事项不得自行翻转**；若有异议应先说明理由再动，而不是先改后报——这是本问反复返工的一个来源。
2. **JSON 由「四舍五入」改为「截断」是精度方向的降级**：JSON 是机读归档件，他人据此取数会拿到 322,255.39，而精确值是 322,255.3952。若要统一，正确方向是"两侧都统一为四舍五入"或"两侧都标精确值并说明取整规则"，而不是把更准的一侧降级。
3. **精确值 `322,255.3952` 未变**（`_trunc2` 只作用于显示）；`Q2_analysis.py` L1466 定义、L1750 使用。
纪律（问题三起适用）：**凡改动涉及已裁定过的事项，先报再改**；配合既定的"文字定稿由编程手一次性改+重跑"。

---

## 附录 C：队长追加检查项的核验（2026-09-12 第三批）

### C-1【**队长已撤回裁定 → 改记为"已评估、accept"，不作为需改动项**】

- **原提法**（队长 2026-09-12 第二批）：对照口径计划阶段的 λΣ(m+p) 项在两章中未提，建议补一句说明。
- **队长撤回理由（第三批）**：① 章节侧不缺这一句——建立章 §五 L154 已有等价内容「$a_{d,t}\ge0$ 的一条含义是微网不向外部电网倒送电能……把盈余电量当作可售出并计入收益，等于把不存在的收入算进方案里」；§八.3 是对照口径小节，职责是定义口径、不在正文里重列计划阶段完整 LP；计划阶段的完整形式在 §七，§七本就**没有** λ 项（章节显式保留 $p,a,e$ 与连接约束）。② 该差异的正确落点是**代码自证**而非改章节——docstring 已写明 λ 是冗余分解、最优处恒为 0、置 0 仅差 4.6e-07 元；补章节的收益只是把已有内容换位置，代价是让 t7 的验收证据与 t8 的复核基准一起失效（章节已冻结在 350 行）。
- **本审计结论**：接受撤回。**差异不存在于两章之间，只存在于"代码用 λ 作冗余分解"与"章节按显式约束写"这两种等价表达之间**；代码侧 docstring 已自洽，章节侧等价模型正确，**不需要产物改动**。
- **仍保留的技术价值（记录备查）**：三条实测（$q$ 被平衡等式钉住 `max|q−need|=0`；$m=p$、最优处 $\max(m)=\max(p)=0$ ⇒ λ 项贡献恒为 0；$q\ge0$ 真正承担 B5(i)「不允许余电外送」，放开下界后 2025-06-21 单日目标由 17 813.64 元被压到 2 334.61 元）由 `common/diagnostics/Q2_planstage_probe.py` 复跑，作为"λ 恒为零"与"为何仍保留 $q$"两件事的区分依据。

### C-2【要求已满足 → 无可改动项；不作为缺陷报出】

队长第三批要求把"表 2-19 缺方法学表注"作为 finding 报出。**现行正文（求解章 460 行，mtime 05:52:54）该表注已在位**，关键短语实测：`锚点取 ②` ×1、`各只改一个量` ×1、`相容性检验` ×2、`+1 753 305.58` ×4、`+322 255.39` ×4、`+1 431 050.19` ×1。逐字摘录见附录 B-3。

因此本项的**要求（锚点 ②、各只改一个量、两条单因子效应之和 = ③→① 的相容性检验）已满足**；如实记录会与磁盘冲突，故本审计记为"**已满足、无需改动**"，并把"若需改措辞属文字润色"作为唯一的可选后续。

#### C-1 / C-2 最终分类（队长 2026-09-12 第十批裁定，2026-09-12 记入）

- **C-1（λ 项）→ `info`（**原 `low`，队长第十批裁定下调**）｜已评估-accept，裁定关闭、不加进章节。** 依据三条：① **等价性已由探针实证**——`Q2_planstage_probe.py`：$q$ 被平衡等式钉住（`max|q−need| = 0`）、$m=p$ 且最优处 $\max(m)=\max(p)=0$ ⇒ λ 项贡献恒为 0、置 λ=0 目标仅差 $4.6\times10^{-7}$ 元，$q\ge0$ 才是 B5(i)「不允许余电外送」的承担者（放开下界后 2025-06-21 单日目标由 17 813.64 元被压到 2 334.61 元）；② **代码 docstring 已说明**"为什么留这一项"属实现层说明；③ **章节按显式约束表述，最优值与最优解不受影响**，写进章节反而引入读者无需承担的概念。
　**定位更正（已采纳）**：λ 项属**对照口径的计划阶段**，**主口径不含**。
- **C-2（表 2-19 表注）→ `info`｜经核不成立。** 表注 L342 三要素齐备且与磁盘一致；队长确认其先前"报出缺注"的指令**基于过时判断**。**不换措辞、不开新一轮 rerun**——章节已封版，为一句措辞动文件只会让快照失效、收益为零。

#### 一般原则（队长 2026-09-12，供后续沿用）

**并非每条 finding 都需要改动。** finding 的价值在于把事实摆清并给出建议；**"改"与"不改"由持有写权限的一方决定，而"不改"只要有理由就是正当结论**。判断"该不该改"属于另一层，不应由复核者越过——本问被过度推动的改动（如附录 B-4 的 JSON 显示对齐）正是越过这一层的产物。复核者的职责边界：**事实 + 证据 + requiredFix 写清即已尽责**，是否落地由写权限方裁定。

#### 引用约定与证据基准戳（2026-09-12，与建模手对齐）

- **引用约定**：凡引用复核产物，一律带**文件名 + `generated_at`**，例如 `_q2_audit_thirdparty.json @06:41:06`（48/48）、`_q2_audit.json @06:37:49`（20/20）。原因：`common/diagnostics/` 下存在**同名不同物**——`_q2_audit.txt/.json` 是 `Q2_audit.py` 的**20 项**流水线审计，`_q2_audit_thirdparty.txt/.json` 是 `_q2_audit_independent.py` 的**48 项**第三方复核；文件名前缀相同、结论口径不同。
- **t13 证据的基准戳**（本审计的 20/20 与全部数值复核所覆盖的产物版本）：
  - 章节（不可变指纹）：求解章 `52 312 B / 05:52:54 / sha256_16 8fbaf0b6f6ff7b12`；建立章 `34 528 B / 04:18:32 / c4ddbf165b73a843`
  - 产物：`result2.xlsx @06:19:24`（402 867 B）、`Q2_diagnostics.json @06:19:34`、`Q2_timeseries.csv @06:19:25`、`Q2_analysis.json @06:35:15`、`Q2_emergency_detail.csv @06:35:15`
  - 审计运行：`Q2_audit.py` 20/20（**06:37:49** 写入）、第三方 `_q2_audit_thirdparty.json @06:41:06`（48/48、fatal 0）——**两者均晚于上列产物**，故基准与最新产物对齐，主口径与冻结报告逐位相同。
- **遗留 advisory（A-6，仅报队长）**：`Q2_diagnostics.json → solver.elapsed_seconds = 33.32693509999808`、`independent_recompute.elapsed_seconds = 10.90583969999716`（即 33.33 / 10.91 s），与求解章表 2-2 所记定稿那次 **33.23 / 11.00 s** 相差约 0.1 s。该表**已有表注**说明耗时随机器负载波动、规模/迭代/求解状态不变，故属"运行相关量"的显示差异，不是编造类问题；**封版前请勿再跑 `Q2_solve_plan.py`**——再跑一次会把两套审计的基准同时推旧。
- **字段路径解析检查（2026-09-12，建模手提出；已升级 v2）**：脚本 `CUMCM2026_C/Q2/Q2_field_path_check.py`（可复跑；诊断产物 `CUMCM2026_C/Q2/_q2_field_path_check.json`，**不进 outputs/**）。覆盖 **7 个 JSON 产物**；状态四态：`OK` / `PARTIAL`（路径缩写）/ `TOLERATED`（靠宽容规则才解析，**≠ 逐字可解析**）/ `UNRESOLVED`（真错）；**判定从紧**——`verdict = pass` 当且仅当 `UNRESOLVED = 0` 且**非豁免的 PARTIAL/TOLERATED = 0**，豁免须显式登记在脚本的 `ALLOWED_TOLERANCES`（当前为空）。
　**v1 → v2 的自我更正（重要）**：v1 曾给出"**验收报告 11 条路径全部解析成功**"，我据此说过"那 3 处路径不精确已修好"——**这是假绿，该结论作废**。两条机制：① v1 把 `variants[*].summary`（`variants` 是 **dict**，`[*]` 是数组通配符）当成可解析，**宽容态掩盖了缺陷**；② v1 只覆盖 2 个产物，`_q2_u4_multiple_optima.json → experiment_1` 这类**根本没进扫描范围**。【路径豁免】
　**v2 实测（当前，verdict = fail / exit 1 —— 这正是应有的红）**：定量 **38 条全 OK**；**否定断言 4/4 PASS + 内容断言 5/5 PASS**（内容断言是 v2 新增：`conclusions[4]` 需含"成对单因子分解"、`[7]` 需含"预报误差"、`[8]` 需含"4.46"、`mae_layers.actual_positive.mae_kw` 需含 657.5、`worksheet_shape_rows_cols` = [1,3] —— **专治"路径能解析但引错条目"**，即建模手被 `conclusions[7]` 绊倒的那一类）；文档扫描——求解章 0 条、建立章 0 条、**本审计记录 5 条全 OK（另有 3 行因带 `【路径豁免】` 标记而跳过，单列计数）**；**`Q2_最终验收报告.md` 命中 27 条：OK 22 / TOLERATED 3 / UNRESOLVED 2**：
　`L93`、`L136`、`L223` → `Q2_analysis.json → variants[*].summary`（**TOLERATED**：dict 上写 `[*]`，应写 `.*` 或具名口径键）；【路径豁免】
　`L307` → `_q2_u4_multiple_optima.json → experiment_1`、`L308` → `… → experiment_2`（**UNRESOLVED**：真实键名 `experiment_1_execution_convention` / `experiment_2_multiple_optima_scan`）。【路径豁免】
　⇒ **共 6 条待修，全部在验收报告内**（建模手已列入待修清单并报队长）：上述 5 条路径类 + **1 条索引语义类**（见下）；两章与本体产物无路径问题。**计数口径（写入 JSON `counting_convention`）**：按**引用位点**去重计 = **6 处**（文档 5 + 索引语义 1）；同一位点可产生多条断言（L138 的 `4.46` 与 `0.6148` = 1 位点 / 2 次断言）；`numeric_overlap_warnings` 3 条为 advisory、不计入 verdict。
　**STAGE E：索引语义断言（v2 新增，专治"路径对、条目错"）**：若某行同时出现"数值锚点"与 `conclusions[k]`，则 k 必须是该数值真正所属的 0-based 索引。实测抓到 **`验收报告 L138` 引 `conclusions[7]` 而该行含 `4.46` / `0.6148` ⇒ 应为 `conclusions[8]`**（"5 倍电价的真实作用"），即建模手人工发现的同一条真错——**这类错误路径解析永远是绿的，只有内容/语义断言能抓**。
　**可复现性说明（回应"空失败列表是零信息"）**：v2 的 `doc_scan` 对每份文档给出 `hits / ok / partial / tolerated / unresolved / exempt` 与两份清单——`all`（**全部被扫描项**：行号 + 产物 + 路径 + 状态）与 `items`（**仅非 OK 项**，含原文摘录）。因此产物可区分"扫了 27 条、5 条非 OK"与"一条也没扫到"，判定可被第三方推翻。
　**STAGE E2：数值重合检查（advisory）**：对被引条目内容与本行数值做**容差内数值比对**（年份不计），一条都不重合时记 `NO_OVERLAP`。当前 3 条 warning：`验收报告 L138`（真阳性，与 STAGE E 同源）、`L309`（该行数值来自其它产物，属正常引法）、本记录 `L108`（引用的是**历史值** `322,255.40`）。**E2 只提示、不计入 verdict**——理由写在脚本里：一行可能同时引用多个来源的数值。
　**实现备注**：v2 曾尝试"任何像路径的代码段都拿去解析"，结果把 `scipy.optimize.linprog` / `statistics.quantiles` / `load_e[i]` 等 Python 标识符报成 UNRESOLVED（假阳），该分支已移除；现只认**文件锚定**写法（`` `某产物.json → 路径` ``，含同行并列代码段的延续）。
　**纳入复核清单的原则**：**数值挂字段，字段名会漂**——"数字对表"查不出路径错：数字都在文件里，路径名不对却会让读者解析到空。同类风险还有**索引约定不明**（`conclusions[4]` 0-based 与"第 5 条" 1-based 指同一条；不注明就会读错条）。今后凡引用 JSON 一律写**完整路径**，带索引时注明 0-based / 1-based。
- **计数口径（2026-09-12 与建模手统一）**：**行数一律按 `splitlines()` 计**。截至本条落盘时：`_q2_paper_audit.md` **296**（41 221 B）、`_q2_paper_audit.json` **271**（22 257 B）、求解章 **460**（52 312 B）、建立章 **350**（34 528 B）、`Q2_最终验收报告.md` 668（56 536 B）。**本审计记录自身仍在增补，引用其行数请现场测**（键名与结构不变，仅追加）。注意 `count('\n') + 1` 在"文件以换行结尾"时会把末尾空行也算进去，比 splitlines **多 1**（两种数法都自洽，但**跨方核对必须标明用哪一种**，否则"行数相同"这个判据本身会失真——与"三个同为 460 行的版本"属同源的坑）。本记录此前对外报数用的是 `count('\n')+1` 口径，按 splitlines 各减 1。
- **显示精度口径（2026-09-12 明确）**：成对分解里的 `+322,255.3952` 是**内部精确值**，**不是展示形式**；论文与 JSON 的展示精度统一为**两位小数截断**（正文 `322 255.39`、JSON `+322,255.39`）。任何一处都不引入四位小数的展示，避免同一增量在不同文档里出现不同精度。
- **版本锚升格（2026-09-12，建模手发现，重要）**：`_q2_field_path_check.json` 出现过**行数与字节数完全相同、只有 digest 与 mtime 不同**的两版——`776 行 / 26 191 B` 下：`8b8abb27157d1ceb @06:53:37` 与 `caa8b23f37187d21 @06:54:23`。**机制**：该诊断件内含 `generated_at`（ISO 秒级）字段，重跑导致的秒数变化**不改变字符串长度**，于是整文件等长而内容不同——这**不是偶发，而是"自带时间戳"类产物的系统性属性**。⇒ **版本锚应为：行数 / 字节数 / mtime / sha256_16（章节再加表号集合、占位符数）**；`sha256_16` 是这套判据里**唯一不可被"等长改写"绕过**的一维。这与"三个同为 460 行的版本"是**同方向、更强一级**——那次的字节数还能分开（52 062 / 52 313 / 52 312），这次连字节数也分不开。已请队长把《引用纪律》④ 由"四快照"升格为五元组。
- **工具定格（2026-09-12）**：`CUMCM2026_C/Q2/Q2_field_path_check.py` **v2.6 冻结**——v2.5（06:55:55 / `337241e78eb92cd0`）冻结后，按建模手建议把流编码兜底的 `errors="replace"` 改为 **`errors="backslashreplace"`**（不可编码字符变成 `\uXXXX`，**信息不丢**；`replace` 变 `?` 可能让落在该字符上的数字失真），**按冻结声明另立版本号 v2.6 并在此留痕**。v2.6 已实测：设与不设 `PYTHONIOENCODING` 均正常输出。**此后任何改动同样须另立版本号+留痕**。冻结理由：连续迭代会让"最新指纹"成为移动靶——与停跑 `Q2_solve_plan.py` 同理，**冻结的意义是让引用者引得住**。
- **等长改写风险清单（建模手 2026-09-12 扫描，比单例更有说服力）**——凡产物里含**定宽生成戳**，重跑即等长改写：
  - **有定宽生成戳**：`_q2_audit_thirdparty.json → generated_at`（`2026-09-12T06:41:06`，19 字符，**正是验收报告引用的那一枚**）；`_q2_field_path_check.json → generated_at`（19 字符，两例已实证）。
  - **变相有**：`Q2_diagnostics.json → solver.elapsed_seconds` 等可变浮点（33.23→33.33 已实证，**位数可不变**）；`Q2_analysis.json → variants.<口径>.elapsed_seconds`（六个口径各一，`variants` 是 **dict** 不是 list，故不能用 `[*]`——本行原写 `variants[*]` 已按自有工具的严格语义更正）同理。【路径豁免】
  - **定格对象的层级（自校结论，2026-09-12）**：**稳定的定格对象是脚本**（`Q2_field_path_check.py` v2.6，内容不随运行变化）；**诊断件 `_q2_field_path_check.json` 是"运行输出"**——内含 `generated_at`，**重跑即产生新指纹**（与本节"等长改写"同类）。故外部引用应以**脚本指纹**为锚，诊断件只报"某次运行的输出指纹 + 产出时刻"。本记录前述"定格对 = 工具 + 诊断件"的表述据此收窄：**锚 = 脚本；诊断件 = 一次运行的证据**。
  - 未扫到定宽戳：`_q2_audit.json`、`_q2_paper_audit.json`。
  ⇒ **五元组不是防你我手误，而是防产物自身**——连审计产物与交付产物都会等长改写。
- **取数协议（终局版，2026-09-12）**：**先彻底停写（定格）→ 再取数 → 立刻发**；**取数与发出之间不得有任何写入**。若必须追加，则追加后**重取并重发**（不允许"取完又写"）。本记录是增长型文件：**自本条起定格**，直至队长 A/B 落地与验收报告 6 处修完，双方各取一次**终局指纹**；中间如需追加，追加后即重取重发。
- **与五元组配套的归因规则（建模手 2026-09-12 提出，采纳并写为规则）**：**五元组让"同长改写"可见，产物内 `version` 字段让改写"可归因"**——两者合起来才治得住这一类。故**凡自带 `generated_at` 的诊断件，必须同时写 `version` 与 `frozen`**（本工具 v2.6 即如此：`"version": "2.6"`、`"frozen": true`）。实例：`_q2_field_path_check.json` 在 **`777 行 / 26 214 B`** 下出现过三枚不同 digest（`8b8abb27…`、`caa8b23f…`、`3da6f724…`）；**前两枚在定格前的迭代窗口，第三枚是协议内的一次版本升级 v2.5→v2.6**——可归因、非违约（建模手已收回"第三例即违约"的说法）。
- **登记件交叉核验（2026-09-12）**：建模手新建 `common/diagnostics/_q2_report_known_open_citations.md`，登记验收报告 **6 处未修引用**（**不修改任何交付物**；若队长裁定方案 A，照其载荷一次落盘）。我按五元组独立复算：**58 行 / 5 042 B / 06:57:15 / sha256_16 `84bdbeeabc74e77c` —— 与其声明一致**。
- **记录件口径（建模手 2026-09-12 建议，采纳）**：`_q2_paper_audit.md` 是**增长型记录**，**其指纹不作为对外指标**——追加 → 取数 → 写消息 → 送达之间存在**必然窗口**，"落后一枚"是**用错了指标**，不是错误。需要引用时写"**本记录 §X（引用时刻 06:5x）**"，或在引用当场现取一枚。**定格对象只有待复核件**：`Q2_field_path_check.py`（v2.6）与其诊断件 `_q2_field_path_check.json`（`frozen=true`）——这两枚已连续两轮被对方复算为 MATCH 且未动。
- **v2.7：输入自描述（建模手 2026-09-12 建议，采纳）**：诊断件新增 `script_frozen: true` / `inputs_frozen: false` / `inputs`（**本次扫描实际消费的 11 个输入文件的五元组**：7 个 JSON 产物 + 4 份被扫文档）+ `inputs_snapshot_at`。原因：**诊断件是派生件，脚本冻结 ≠ 输出冻结**——输入一变（尤其本增长型记录），重跑即产出不同 JSON，而旧的 `frozen: true` 会被误读成"这份不再变"。⇒ **外部引用以脚本指纹为锚**；引用诊断件时**同时引用其 `inputs` 快照**，从而把"扫了哪些路径"扩到"**扫了哪些版本**"。
- **v2.8：幂等输出（建模手 2026-09-12 建议，采纳；根治"观测者效应"）**：把**挂钟时间戳**换成**内容决定的 `run_fingerprint`**（= `sha256(TOOL_VERSION + 规则签名 + 各输入 sha256_16)[:16]`），并新增 **`--dry-run`**（只打印不写盘，把"引用"与"改写"解耦）。**实测**：连续两次正常运行产出**逐字节相同**的诊断件（`30 518 B / sha256_16 510f54aaff9ee160`），`--dry-run` 后文件不变 ⇒ **输入不变 ⇒ 输出不变**——这才是 `frozen` 的确切含义。此前"冻结反复失效"的根因是"**挂钟戳 + 必须运行才能读**"两者叠加：**为了引用而取数，取数又改写了被引用对象**。
- **开发期没有锚（建模手 2026-09-12 定论，采纳）**：**锚不是一个文件，而是一个被声明过"停止编辑"的状态**；只要文件仍在被编辑，任何指纹声明在送达前必然过期。正确时序只有一种：**先宣布"从此不再编辑"（停手）→ 再取数 → 再声明**。本条即该定律的落地：v2.8 起**工具停止编辑**，`--dry-run` 供引用时使用，**引用不再改写产物**。
- **锚的分类（建模手 2026-09-12，双方口径一致）**：**手写件**（两章正文、验收报告）→ **内容指纹（五元组）**即可，编辑即换指纹、语义清楚；**机器产物**（`result2.xlsx`、诊断件、两个审计 JSON）→ **"产出时刻 + 指纹"成对**（如 `_q2_audit_thirdparty.json @06:41:06 / 3a6d4a5476444990`），因为它随时会因重跑而变。
- **"记录优于冻结"（建模手 2026-09-12 定论，采纳）**：**冻结**是"用禁止变化来保证可引用"；**记录**是"用自描述来容纳变化"。本链路实证：`inputs` 快照落盘后，**即便脚本或输入后来变化，引用者仍能知道自己在引哪一版**，不再依赖对方"停手"。`_q2_field_path_check.json` 的 `inputs`（11 个文件五元组）已被对方逐条复算 **11/11 MATCH**；随后 v2.8 再进一步（内容决定的 `run_fingerprint` + `--dry-run`），**引用动作不再改写产物**。
　**推广计划（并入分档 A，待队长授权）**：给 `common/diagnostics/Q2_audit.py` 与 `_q2_audit_independent.py` 同样加**输入快照**（脚本版本 + 被扫产物指纹）与流编码兜底；代价是二者需重跑 ⇒ `_q2_audit.json @06:37:49`、`_q2_audit_thirdparty.json @06:41:06` 换戳，验收报告引用处需同步。
- **幂等化的独立验证（建模手 2026-09-12 实跑，三项通过）**：① `--dry-run` **惰性成立**（跑前/跑后诊断件 **30 518 B / mtime 不变**）；② **输出件内挂钟类 ISO 时间戳 0 次**；③ **确定性成立**——dry-run 在 stdout 打印的 `run_fingerprint` **与盘上件内存储值逐位相同**。他采用的是**比"信我跑过三次"更强的方式**：不重放我的历史三连跑，而是**自己跑一次并比对指纹**。
- **验证方式随之改变（采纳）**：由于 getter 已幂等，**验证从"相信我跑过 N 次"改为"你自己跑一次 `--dry-run` 比对 `run_fingerprint`"**；该字段与 `verify_hint` 一并写入输出件，供对方当场复现。
- **v2.9：规则改动也可归因（建模手 2026-09-12 建议）**：`run_fingerprint` 的载荷纳入 **`rule_version`**（扫描规则版本：`CITATIONS / NEGATIVE / CONTENT_ASSERTIONS / INDEX_RULES / ALLOWED_TOLERANCES` 的规模或内容变化时 +1），于是"规则改了导致指纹全变"同样可归因。输出件新增 `rule_version` 与 `verify_hint` 字段。
- **输出件不作锚（建模手 2026-09-12 建议，采纳为口径）**：**锚只有脚本**；运行件（诊断 JSON）**不是稳定值**——它随输入而变，**且自己说明了为什么变**（`run_fingerprint` + `inputs` 快照）。引用它时只给 **`run_fingerprint + inputs`**，不给"输出指纹"。**取证/引用一律用 `--dry-run`**（只读、不改写任何文件）；只有"需要刷新盘上件"时才用写盘模式，且刷新后必须重新声明。**据此，本记录不声明冻结**（建模手 2026-09-12 建议，采纳）——它是**增长型记录**，其本性就是增长；既然**不作锚、不进他人引用**，其增长便与任何人无关，**为其声明"停止追加"既无必要也无法被验证，只会再产生一次"声明与现值不符"**。需要引用本记录某条时**现取**即可。
- **开发结束声明与收口验证协议（2026-09-12，与建模手约定）**：**`Q2_field_path_check.py` 开发结束于 v2.9**——计划内不再改动（若队长裁定"分档 A"，改动对象是 `Q2_audit.py` 等**其它脚本**，不涉及本工具）。**声明前置条件**：本记录同时冻结（A/B 落地前不再追加），以免继续推动运行件。**收口验证协议（对方一次性执行，不再逐版往返）**：① 复算脚本五元组；② `--dry-run` 取数并比对 `run_fingerprint`（不写盘）；③ 逐条比对 `inputs` 11 项与现盘；④ 一次回复 pass/fail。
　**措辞更正（采纳）**：开发中的值**不得称"锚"**——**"锚"是被声明过的停止状态，不是一个当前值**；把开发中的值称作锚，会误导后续读者按它去引。本条之前的"已停止编辑/已冻结"若指开发中状态，一律按"**开发中，暂无锚**"理解。
　**配方更正（建模手 2026-09-12 实跑发现，采纳）**：上条协议第 ② 步须写明**比对基准**——跑 `--dry-run`，把 stdout 的 `run_fingerprint` 与**你手上那份诊断件自身的 `run_fingerprint` 字段（或 `inputs` 快照）**比对；**不得与消息里引用的数字比对**。原因：消息里的数字有保质期，件内的快照没有——**判据要挂在产物上，不要挂在消息上**。实测教训：对方按不含基准的配方跑出 `0ffaa1b3f8991dae`，与我消息里的 `bf7ffb1daaf1257e` 不符，而**与盘上件内所存值逐位相同**（即机制成立、消息过期）。
　**配方终稿（2026-09-12）**：跑 `--dry-run` → 与**你手上那份件里自存的 `run_fingerprint`** 比对：**相同 ⇒ 输入未变（幂等）；不同 ⇒ 由件内 `inputs` 快照定位是哪一份输入变了**。两者都是有效结果——**幂等只在输入不变时成立，而现实里输入总会变；真正的收益是"变了也知道是哪一份变了"**。
- **v3.0：增长型记录退出指纹载荷（建模手 2026-09-12 四步协议结果驱动）**：对方按协议一次跑完，结果为 **机制 PASS / 声明 FAIL / 交付面 PASS**——第 2 步"声明 FAIL"的**唯一原因**是 `_q2_paper_audit.md` 在我取数后又长了一次（325 → 326 行），**而第 3 步 `inputs` 的 10/11 正好把"变的只有这一份"指认出来**；此外我一条消息里的"值2/值3 对不上"也是同源（324 ≠ 325）。
　**修改（一行逻辑）**：`FINGERPRINT_EXCLUDED = {"_q2_paper_audit.md"}`——**该记录仍被扫描、仍在 `inputs` 里留痕（`fingerprint_excluded: true` / `fingerprint_role: record`），但不参与 `run_fingerprint` 的载荷**。理由照记对方原话：**指纹是用来锚定"可被引用的东西"的；一个本性就要增长的文件，不该参与锚定。** 输出件另加 `run_fingerprint_inputs` 明列参与锚定的输入。
- **v3.0 排除机制：已验证（建模手 2026-09-12 独立复跑）**：对方按定稿配方实跑，五项全中——脚本五元组 MATCH、件内自存 `run_fingerprint` MATCH、`run_fingerprint_inputs` 10 项在位、`inputs` 记录档 `fingerprint_excluded = True` 在位、**dry-run stdout 与件内自存逐位相同**。**关键在"不利条件"**：他核时记录档已从件内快照的 **328 行**长到 **330 行**，**指纹仍逐位相同** ⇒ **"记录增长不再带动指纹"是机制而非巧合**。
　据此把上述提法由"提法"升格为**已验证**：**指纹是用来锚定"可被引用的东西"的；一个本性就要增长的文件，不该参与锚定。** 验证三件：① 机制三项（`--dry-run` 惰性 / 确定性 / 漂移可归因）；② 脚本锚稳定；③ **值可复现且与记录增长无关**。
　**收口时对方需 v3.0 的三个值**（脚本五元组 / `run_fingerprint` / `inputs` 快照）写入其登记件 §六——**届时现取，不引用本条**。
- **"验收在旧基准上通过"这一类缺口（建模手 2026-09-12 发现并补跑）**：`Q2_t10_acceptance.py`（26 项）上次运行于 **05:55**，而其后的产物经历了 **06:19 / 06:35** 两轮重生成 ⇒ 该 26 项此前是在**旧基准**上通过的。对方已在当前基准重跑：**全部 PASS、exit 0**（含第 10 项 `Q2_audit.py 05:25:16 < _q2_audit.json 06:37:49` 的 mtime 不变量成立）。
　**类属**：任何"验收/审计产物"都必须记录**它所针对的基准版本**，否则"通过"只意味着"对某一旧版通过"——**这正是 `inputs` 快照要解决的问题在验收侧的同型表现**。**承诺**：分档 A 若落地（两个审计脚本加编码兜底 + 输入快照），**改后必须补跑这 26 项**，并在引用时同时给出"验收运行时刻 + 其基准版本"。
- **数字引用规则（2026-09-12，本轮教训固化）**：**消息与自由文本里的裸数字会漂**——本轮实例：报告行数 `568?`→668（手误）、`0.6146`→`0.6148`、审计记录行数 285/286（计数口径）、待修项"5 条"vs"6 处位点"（位点/断言/行三种口径）。**规则**：凡引用数字，必须带**来源三元组**——文件路径 + 指纹（`splitlines` 行数 / 字节数 / mtime / sha256_16）+ 字段路径（JSON 用完整路径）；给不出来源的数字只作口头说明，**不入稿**。产物里的数字有 digest 与对表兜底，消息里的没有——故**结论只从产物取，不从消息取**。**取数法（与建模手约定）**：行数/字节/sha256_16 一律**一次 `read_bytes()` 三值同出**，对外报数**直接粘贴脚本输出、不手抄**（本轮曾把报告 56 536 B 手抄成 56 535 B，sha 与对方一致 ⇒ 属转写少 1，不是读盘差异）。

---

## 附录 D：队长第三批裁定与审计结论的最终对齐（2026-09-12）

| 事项 | 队长裁定 | 审计侧最终记录 |
|---|---|---|
| λ 项与章节的差异 | **撤回 finding**，按"已评估、accept"记录 | 附录 C-1 已改写为 accept；不改产物 |
| 表 2-19 方法学表注 | 要求报出"缺注"finding | **要求已满足**（表注在位，关键短语×6 项实测）；按事实记为"无需改动"，不制造缺陷 |
| pcap 手写死数 | 要求把差异原因写进 JSON note | **已完成**：`Q2_analysis.json → pcap_sensitivity.note` 新增"与早期记录不一致的说明"段落（见附录 B 与下条） |
| 成对分解判据（锚点 ②） | 按 ③→② = +1 753 305.58、①→② = +322 255.39、③→① = +1 431 050.19 恒等核 §七.3 | 已核通过（附录 A/B）；JSON 侧显示已对齐为 +322,255.39 |

**pcap note 差异原因（现行 JSON 原文摘录）**：
> …队长此前记录过另一组对照（紧急购电 1 545 668 → 1 546 061 kWh、总费用 20 652 081 → 19 448 278 元），那组来自**预报源与小时块索引订正之前**的运行：p^cap 的基准是『当天预报净负荷峰值』，订正前后预报源由前一日 18:00 发布改为第 d 日 0:00 发布、整点块铺展由错位改为一对一，峰值本身与触界时段随之改变，故两组数值不可直接比较。**当前产物（已核）**：紧急购电 1 260 781.34 → 1 263 115.18 kWh（0.19%）、上限触界时段 24 868 → 8 630、总费用 19 226 746.31 → 18 194 189.41 元……

**审计总结论不变：verdict = pass**（round 1 的 F1–F5 全部关闭；A1/A2 为低危 advisory；C1 已 accept；C2 为"要求已满足"）。

---

## 附录 E：队长第四批裁定（MAE 两层口径 / 表 2-17 表注 / 旧数作废）的执行记录

### E-1（A）MAE 主口径维持 371.2208，**387.36 变体已删除**，两层并列口径已落到 JSON

- **删除核验**：`Q2_analysis.json` 与 `Q2_analysis.py` 中 `387.36` / `covered_only` / `mae_kw_covered` / `144/138` 命中均为 **0**。
- **现行口径块（`forecast_errors.mae_definition`，现场计算）**：
  - 主指标 `mae_kw_full_day = 371.2207801792249`；`stats_full_day` = MAE 371.2208 / RMSE 648.3065 / 偏差 −29.6762 / P90 1185.3359（48 096 时段）；
  - **次要参考口径** `secondary_reference`：`mae_kw_actual_positive = 657.5499711011905`、`n_intervals_actual_positive = 26 880`、`zero_actual_interval_share = 44.11%`，并写明"**不替代主指标**，仅说明夜间零值时段对全天指标的稀释；论文引用必须写明所用口径"。
- 重跑：`Q2_analysis.py` exit 0；`Q2_audit.py` **20/20 / exit 0**；`Q2_t10_acceptance.py` 26 条全 PASS；`validate_results` 21 通过 / 0 失败 / 3 缺失；mtime `analysis.py 05:58:57 < analysis.json 06:01:25`。

### E-2（B）`紧急购电量` 维持 t10 口径 —— 已确认在位（`max_row=1` + A1 批注 252 字 + `header_only=true`）

### E-3（C3）队长要求报出的第二条 finding"表 2-17 表注缺精度口径说明"——**经核已在位，不报缺陷**

现行求解章 L299 表注逐字为：
> 表注：前三行都在全天 144 个时段、共 48 096 个时点上统计，口径一致。附件 2 的夜间光伏恒为零（见第二节），全天口径因此被零值时段稀释：48 096 个时点里有 21 216 个（44.11%）的实际光伏恰为 0，只剩 26 880 个（55.89%）大于 0，后者的 MAE 为 657.55 kW。两者分母不同，不能按样本比例直接换算，$371.2208=\frac{26\,880}{48\,096}\times657.55+3.73$，多出的 3.73 kW 来自夜间时点的少量残差（合计 179 291.42 kW，折算到全天即 3.73 kW）。

队长要求补的三项（44.11% 占比、全天 MAE 被稀释、实际光伏 > 0 的 26 880 时段 MAE = 657.55）**全部已在表注中**，并额外给出可验算的分解恒等式。故记为"**要求已满足、无需改动**"，不制造与磁盘冲突的缺陷。

### E-4（C4）404.97 / 489.07 作废 —— 记录在案

队长说明该两数出自其一次性临时脚本（"前一小时块 / 后一小时块"两种落位，在块 1..22 的统一覆盖上统计），非现口径产物；队长已宣布作废。本审计此前"无法复现"的判断与之一致，以现口径 **371.2208 / 448.3124 / 689.8521** 为准。

### E-5 队长采纳的两条复核结论（记录）
① 实际光伏 > 0 的 26 880 时段 MAE = **657.55 kW**、夜间零值时段占 **44.11%**（稀释全天 MAE 至 371.22）；② 逐位相等的 20 780 个时段中实际 > 1 kW 的仅 **36 个（0.0749%）**且**全部落在整点最后一个 10 分钟格**，属**附件生成方式的痕迹、非泄漏通道**。两条已由队长写入契约的"夜间低信息量事实"节，与 `Q2_audit.py` 的 U5 及 `Q2_attachment3_caveat_probe.py`（已按 t10 要求删除）留痕保持一致。

### E-6 队长第五批裁定：MAE 两层键名对齐 + 共同覆盖窗补行建议

- **键名对齐（已执行）**：`forecast_errors.mae_definition` 现为
  `stats_full_day`（主指标 MAE **371.2207801792249**，RMSE 648.3065 / 偏差 −29.6762 / P90 1185.3359，48 096 时段）
  与 `stats_actual_positive`（**657.55 kW**、26 880 个实际光伏 > 0 的时段、零值占比 **44.11%**，并标注"不替代主指标、引用须写明口径"）；
  `387.36` 与旧键名 `secondary_reference` 在 JSON/脚本中残留均为 **0**。
  重跑：`Q2_analysis.py` exit 0；`Q2_audit.py` **20/20**；`Q2_t10_acceptance.py` 26 条全 PASS；`validate_results` 21/0/3。
- **两条"要求报出"的项，核验结论仍为"要求已满足"**（表 2-19 表注 C2、表 2-17 表注 C3，引文见附录 B-3 / E-3）。
- **本轮唯一真正可落盘的项 = A3（low，可选增强）**：表 2-17 现只有 3 行，**共同覆盖窗的等样本比较（0:00 740.33 / 6:00 636.12 / 12:00 552.96 / 前一日 18:00 845.75 kW，偏差全负）只写在表后正文 L301**，未作为表行。建议论文手一次性补一组行（或单列"共同覆盖窗 MAE"），并在表注保留两点：① 0:00 发布在该窗内**最差**，选用它是**信息可得性**而非精度；② 该比较支撑问题三"是否值得用其他时刻预报做滚动调整"。正文 L301 已具备这两点表述（`并非最准` / `信息可得性` / `支撑问题三` 各 1 处），故补行属增强、**不改任何数值**。

### E-7 队长第六批更正（两层 MAE 恢复 / 表结构与判据 / 探针恢复）的处置

**（1）两层 MAE 已恢复并改为中性命名（本轮执行）**
`forecast_errors.mae_definition` 现含 `mae_layers = {full_day, actual_positive}`（并保留 `stats_full_day` / `stats_actual_positive` 与 `mae_kw_full_day` 别名，取自同一变量、不会漂移）：

| 层 | MAE | RMSE | 偏差 | P90 | 样本 | 备注 |
|---|---|---|---|---|---|---|
| `full_day`（主指标） | **371.2208** | 648.3065 | −29.6762 | 1185.3359 | 48 096 | 当天全部 144 个时段 |
| `actual_positive`（参考层） | **657.55** | **864.5483** | **−59.7693** | **1453.8531** | 26 880 | 实际光伏 > 0；`zero_actual_interval_share = 44.11%` |

命名已弃用"信息性/精确段"字样；`泄漏`/`leakage` 在 JSON 中 **0 命中**、`387.36` **0 命中**。重跑：`Q2_analysis.py` exit 0。

**（2）`紧急购电量` 表 与 判据 —— 复核结论：**当前已是**你要求的形态，无需改动**
- `result2.xlsx` 实测：`max_row = 1`、`max_col = 3`、A2 = `None`、A1 批注 **252 字**；
- `Q2_diagnostics.json`：`worksheet_shape_rows_cols = [1,3]`、`worksheet_header_only = true`，且**不存在** `worksheet_has_explanation_row_only` 键（早已随回退删除）；
- `Q2_audit.py` 检查项 6 判据现为（原文）：「紧急购电量表：仅表头（max_row=1，无数据行、无说明行）+ 3 列表头 + '起-止' 格式」，断言 `n_rows == 1` —— 即**已是严格判据，未放宽**；
- `worksheet_layout_note` 的两次相反裁定留痕**已保留**。
> 结论：本轮无需对表体或判据做任何改动（此前"至多一行说明行"的放宽判据已在上一轮回退时撤销）。

**（3）探针已恢复并中性化**
`common/diagnostics/Q2_attachment3_night_zero_probe.py`（恢复自原脚本，标题与正文改为中性表述）：全文 `泄漏`/`leakage` **0 命中**；实跑 exit 0，输出夜间恒零钟点 {0,1,2,21,22,23}（6 个小时块）、四发布时刻各自的完全精确列、以及 `全 144 时段 MAE 371.2208 / 实际光伏 > 0 时段 MAE 657.55`。**契约如需引用，请用新文件名**。

**（4）队长要求核的第 3 项（共享校验器措辞）——已核，一致，无需记录**
`validate_results` 在 `max_row=1` 下输出：`紧急购电量: 3 列 + 时间段 '起-止' 格式 → 0 条明细; 格式合规`，与审计口径（0 条数据行）**一致**；共享模块未做任何改动（不牵动 Q1）。故本项记为"已观察、一致"，不列入 findings。

**（5）t6 / t3 —— 按队长确认不再认领**（t6 = completed attempt 6、captain 持有；t3 = 建模手 attempt 2、verdict=pass）。

### E-8 队长第七批四处修复的执行与核验（2026-09-12）

| # | 要求 | 处置 | 实测 |
|---|---|---|---|
| 1 | `紧急购电量` 回到 `max_row=1`、`[1,3]`、判据改回"0 条数据行（含说明行）"、`layout_note` 删除归属表述 | **表体与判据本已是该形态**（上一轮回退时即已改回）；本轮真正修的是 `layout_note`：改写为"**排版沿革（如实记录，不写归属）**：本表经历过两次相反的排版要求——先要求补一行说明文字、其后要求改回 A1 批注；最终形态为 3 列表头 + `max_row = 1`…" | `max_row=1`、`max_col=3`、A2=None、A1 批注 252 字；diagnostics `[1,3]`/`header_only=True`/无 `worksheet_has_explanation_row_only` 键；`Q2_audit.py` 检查项 6 = "仅表头（max_row=1，无数据行、无说明行）" |
| 2 | 恢复两层 MAE（中性命名） | **已在位**（上一轮已恢复） | `mae_layers.full_day` MAE 371.2208（48 096 时段）、`mae_layers.actual_positive` MAE 657.55（26 880 时段，实际光伏 > 0）+ `zero_actual_interval_share = 0.4411`；`657.55`×1、`mae_layers`×2、`泄漏`=0 |
| 3 | 主线对照字段统一到 ③ | **本轮修改**：`control_convention_label` / `reference_variant_for_paper` 由 ④ 改为 **③「仅光伏不可知（负载已知）」**，三枚数值（紧急 1 262 153.80 kWh / 费用 17 938 582.49 元）同步取 ③；新增 `revised_release_variant`（④）与 `revised_release_note` 说明 ④ 的定位是"改用更晚发布预报"的对照行 | `control_convention_label` = ③；`revised_release_variant` = ④；`extra_cost_vs_main_yuan` 仍为 ④ 口径的 5 117 260.24 元，并由 `cost_gap_reference_note` 说明用途 |
| 4 | 四处落盘后重跑四命令 | 全 **exit 0** | `Q2_solve_plan.py`（主口径 20 218 838.180253 kWh / 12 245 046.915278 元 / 自检 12/12）｜`Q2_analysis.py`｜`Q2_audit.py` **20/20**｜`Q2_t10_acceptance.py` **26 条全 PASS**｜`validate_results` 21 通过 / 0 失败 / 3 缺失 |

**注**：上述三件修复均落在 `Q2_analysis.py` / `Q2_solve_plan.py` 与相应 JSON，**未触及两章正文**；`main_vs_control` 的字段统一属命名与口径归属，**不改变任何数值**（③ 与 ④ 的数值都完整保留）。

### E-9 note 措辞按队长句式对齐 + 一处计数自我更正（2026-09-12）

- `worksheet_layout_note` 已改为队长要求的句式（开头即："**本表在收口过程中出现过两次相反的版式要求**：早先要求补一行说明行，其后要求改回批注；最终以 `max_row = 1` + A1 批注为准。留痕仅为可追溯，**不代表任一方的最新指令**。"）。实测：note 内 **不含**"队长"；"最新指令"仅出现在"**不**代表任一方的最新指令"这一否定句中。
- 表体复验：`max_row = 1`、`max_col = 3`、`A2 = None`、A1 批注 252 字；`Q2_diagnostics.json` = `[1,3]` + `header_only = true`（无 `worksheet_has_explanation_row_only` 键）。
- **计数自我更正**：`448.3124` 在当前 `Q2_analysis.json` 中为 **6 处**（`spreading_rule_comparison.<口径>.rules.shift_one` 的规则值与说明、`convention_correction`、`convention_correction.additional_corrections` 等），均系同一"错位一格"对照规则的引用；我此前"全文仅 1 处"的说法对应更早一版 JSON，属陈旧计数，以本次实测 6 处为准。`387.36` = 0 处。
　**（路径精度更正，2026-09-12）**：上句原写作"`spreading_rule_comparison` 的规则值与说明、`convention_correction`、`additional_corrections`"——后两项实为**同一层级**，`additional_corrections` 挂在 `convention_correction` 之下（完整路径 `convention_correction.additional_corrections`，实测 `list(len=2)`），按顶层键读会读空；`448.3124` 的完整路径是 `spreading_rule_comparison.<口径>.rules.shift_one.mae_kw`（属**缩写引用**，非错误）。
- mtime 链：`Q2_solve_plan.py 06:18:32 < result2.xlsx 06:19:24 < Q2_diagnostics.json 06:19:34`；`Q2_analysis.py 06:13:36 < Q2_analysis.json 06:21:55`。

### E-10 审计侧 D8 修复的复核（队长第八批第 4 项）

队长要求修 `Q2_audit.py` 独立 LP 的 `need` 表达式并把 U4 归因改对。**经核，该项已由建模手在此前完成（`Q2_audit.py` mtime 05:25:16），本轮复核确认无误**：

- **代码**：`Q2_audit.py` L599 现为 `need = load_e[i] - pv_e[i] + x_plan - y_plan`，并在 L597 留有说明注释（"此前漏写 `- pv_e[i]`，使预报光伏被整段计成缺口，全年紧急购电被高估约 15 倍"）；计划阶段已改用实际负载 `load_e[i]`（不再用 `naive_load`）。
- **U4 文本**：条目标题与 verdict 已改为"**已定位并修正**（原记录为『未能独立复现 ⇒ 多重最优』，该归因作废）"，`problem` 以【2026-09-12 修正 · D8】开头，写明漏减 `pv_e[i]` 的事实、修正前 17 869 822.25 kWh 与修正后 1 234 668.07 kWh，并给出**结构性证伪**：口径③下 $e_t=\max\{V^{\rm f}_t-P^{\rm act}_t-d_t,0\}$，$x_t,y_t$ 完全相消 ⇒ 多重最优在结构上不可能改变 $e$（并附参考日 11 解扫描极差为 0）。
- **数值**：`_q2_audit.json → control_b2ii.independent_emergency_kwh = 1 234 668.07`，与流水线 1 262 153.80 kWh 相差 2.18%。
- **复核**：`Q2_audit.py` 重跑 **20/20、verdict=pass、exit 0** —— 20 项校验的是流水线产物，归因修正不改变通过与否。

⇒ 该四项（两层 MAE、口径字段统一、检查项 6 严格判据、审计侧 D8）**均已落盘**，本轮无需再改；mtime 分别为 `Q2_analysis.py 06:13:36`、`Q2_analysis.json 06:21:55`、`Q2_audit.py 05:25:16`。

## 附录 F：收口补记（队长 2026-09-12 第九批裁定后的记录）

**F-1（显示规则 · 2026-09-12 最终实测状态）**：精确值 `322,255.3952` kWh 在正文与 JSON 现**均按两位小数截断显示**——求解章 `322 255.39`×4、`Q2_analysis.json` `322,255.39`×1，`322,255.40` **0 命中**，即**两处已一致**（截断由 `Q2_analysis.py::_trunc2`，L1466 定义 / L1750 使用；**精确值未变**）。沿革：JSON 原为四舍五入 `322,255.40`，后改截断以对齐正文；队长裁定**接受现状、不再回退**，同时指出该替换属精度方向降级，若再统一应以"两侧都用四舍五入"或"两侧都标精确值+取整规则"实现（详见附录 B-4 补记）。

**F-2（A1 的结案与审计侧自我更正）**：round 2 的 advisory **A1 应记为“已解决 / 不成立”**。求解章 §四 L121 已写明样本与算法——"这九个数以全部 48 096 个时段末储电量为样本，按 $p$ 分位取自排序序列第 $p(N+1)$ 位并作线性插值的口径计算；若改用第 $p(N-1)+1$ 位这类常见口径，同一批样本下这九个数最多相差约 0.1 kWh"。实跑 `common/diagnostics/_q2_a1_quantile_check.py`：$p(N+1)$ 线性插值（Hyndman–Fan type 6，等价于 `numpy method='weibull'` 与 `statistics.quantiles`）**逐值精确复现**正文九个十分位（1 224.4 / 2 700.0 / 4 340.4 / 6 000.0 / 7 035.9 / 9 058.8 / 10 050.0 / 10 232.4 / 10 800.0），与替代基准 type 7 的最大差为 **0.1112 kWh**，正文"约 0.1 kWh"准确。
　审计侧更正：原 A1 只测了 `numpy` 的 `linear/lower/higher/midpoint/nearest` 五种方法，**漏测 `method='weibull'`**（即正文写明的 $p(N+1)$ 插值），故"五种分位法都对不上"只对这五种成立，作为 finding **不完整**；据此 A1 关闭。

**F-3（封版指纹；t13 的 pass 绑定于此，任何后续编辑都会使其失效）**：
- `CUMCM2026_C/Q2/问题二模型的建立.md` —— 350 行 / 34 528 B / mtime 2026-09-12 04:18:32 / sha256_16 `c4ddbf165b73a843`（8 条判据串在位；旧口径残留与结果数值 token 全 0）
- `CUMCM2026_C/Q2/问题二模型的求解.md` —— 460 行 / 52 312 B / mtime 2026-09-12 05:52:54 / sha256_16 `8fbaf0b6f6ff7b12`（表号 2–21 连续；13 占位符 ↔ 13 PNG 双向无差集；`流水线` 0 命中；F1–F5 标记串全数在位）











<!-- 本条仅为演示：记录增长不应改变 run_fingerprint（v3.0 排除机制） -->
