# C题版本对照与修正记录

日期：2026-09-12

## 对照范围

本次将 `D:\\astra版本\\数模代码手查收_20260912\\项目文件` 的全部文件与当前仓库逐项对照，范围包括题面、附件 1--5、源码、工具、测试、文档、JSON 中间结果和五份结果表。二进制附件按 SHA-256 比较，文本源码按文件内容比较。

## 数据结论

题面 PDF/TXT、附件 1--4 和附件 5 五个模板与 Astra 快照逐字节一致；当前模型差异不是由数据替换或小数位改变造成的。当前 `data/raw/C题` 是本仓库的只读数据源，运行过程不修改原始附件。用户此前指定的外部源目录 `D:\\数模2026题\\C题\\附件` 在本机当前不存在，因此不能再次对该目录做现场哈希审计；已有审计记录和本次与 Astra 快照的哈希结果一致。

## 发现的主要问题

1. 旧问题 2 求解器把当天实际负荷和实际光伏直接交给 00:00 的 LP，属于未来信息泄漏；同时旧的预测入口借用了附件 3，混淆了问题 2 与问题 3 的信息结构。
2. `alpha=0.825` 曾用评价期实际费用选取，属于同一时期上的调参，不能作为因果预测参数的无偏证明。
3. 旧问题 3 先按普通购电费求解，再事后计算上调/下调费用，调整费没有进入优化目标，因而决策并非题面结算规则下的最优决策。
4. 旧问题 4 将附件 4 全日实际价格直接用于计划；若采用在线信息口径，这同样泄漏未来价格。
5. 旧实现按天重置实际 SOC、只回放计划动作，无法表达跨日连续储能和预测误差下的紧急购电。
6. 旧 LP/导出链路没有稳定执行充放电合计功率约束，且结果表只填模板样例储能行；宽表还用循环移位写入，导致表头和物理时段语义不一致。

## 已合入的修正

- 新增 `src/CUMCM2026_C/dispatch.py`，统一实现历史预测、附件 3 发布预报、分段 LP、调整费用、实际执行、紧急购电、弃光、未用合同电量和结算。
- 新增 `src/CUMCM2026_C/export_results.py`，导出 334 天完整计划/调整表、334×6 个储能区块和动态紧急购电明细，不再循环移动首时段。
- 问题 2/3/4 入口改为共享因果实现；问题 1 保留修正后的确定性 LP 基线。
- 新增 `tools/run_corrected.py`、`tools/verify_corrected.py`、`tests/test_dispatch.py`，并将结果验收器切换到修正版时段口径。
- 计划阶段默认只使用可获得的历史信息；实际 SOC 从 2025-01-01 的 6000 kWh 连续传递，1 月作为预热期，2 月 1 日至 12 月 31 日计费。

## 使用项目虚拟环境重算的结果

项目 `.venv` 使用 `scipy 1.18.1`。滚动 LP 存在数值等价解，系统 Python 的旧 SciPy 版本可能给出不同但可行的多解；论文和最终表格统一使用 `.venv` 重算结果。

| 情形 | 计划购电量 (kWh) | 最终购电量 (kWh) | 紧急购电量 (kWh) | 总费用 (元) |
|---|---:|---:|---:|---:|
| 问题 2 | 21,732,963.484267246 | 21,732,963.484267246 | 681,808.6790968967 | 15,930,948.047647865 |
| 问题 3 | 21,083,777.209809642 | 21,543,688.583518 | 1,028,128.7123150768 | 17,787,951.83659296 |
| 问题 4-2 | 21,733,614.03176117 | 21,733,614.03176117 | 681,456.1830646109 | 16,849,791.086142633 |
| 问题 4-3 | 21,083,622.247487277 | 21,541,860.391988397 | 1,027,064.4600081125 | 18,691,787.177165993 |

问题 1 修正版典型日基线：购电量 `59,482.69899835392 kWh`，购电费 `35,126.948589289634 元`，限光为 `0 kWh`。它是附件 1 典型日结果，不能直接外推为全年费用。

## 验证结果

- 行为测试：7 项全部通过。
- 独立物理/结算验算：问题 2、3、4-2、4-3 共 `192,384` 个时段全部通过；最大残差约 `1e-11`。
- 结果表验收：5 份工作簿 `67` 项通过，失败 `0`，缺失 `0`。

## 仍需在论文中明确的假设

历史窗口 28 天、负荷 0.8 分位、历史光伏 0.2 分位、价格历史中位数、光伏缩放系数 1.0 和日终计划 SOC 6000 kWh 是固定示范参数，尚未做时间外推的训练/验证/测试选参。问题 4 的结果采用“计划时只知历史价格、实际结算使用附件 4 价格”的在线口径；若官方明确附件 4 可在日初完全预见，应另外报告完全预见下界。预付计划、未用合同电量不退款以及模板时间列瑕疵也必须在论文中注明。
