# 结果文件模板口径变更说明（2026-09-13）

本次变更将 `results/CUMCM2026_C/` 五个结果工作簿的时段标签与数据座次从"物理区间口径"切换为**官方附件5 模板原样口径**。数值零重算，仅标签与单元格座次变化。

## 一、背景与根因

- 官方模板（附件5）的 144 个时间段列从 `0:10-0:20` 开始、`0:00-0:10+1` 结束；我们此前导出的是 `0:00-0:10 … 23:50-0:00+1`（物理区间标签），与模板字符串不一致。
- 根因是模板固有的**左移一格瑕疵**：附件时间列本为"时段终点"标注（`0:10` 表示 0:00–0:10 时段），模板制作时把终点序列直接拼成区间串，导致模板第 i 列的字面标签实际描述的是区间 i+1。任何与模板逐字符一致的结果表都绕不开这一错位。
- 仓库原有两套成对的写盘口径（`io_attachments.write_plan_columns`，裁定 A18 的唯一入口）：`"decision"`（物理标签，数据位 i = 时段 i）与 `"template"`（模板原样标签，数据位 i = 区间 i+1）。本次按用户裁定切换到 `"template"`。

## 二、具体改动内容

| 对象 | 改动 |
|---|---|
| `src/CUMCM2026_C/export_results.py` | 宽表（计划购电量/调整购电量）表头改为 `write_plan_columns("template")` 的模板原样标签（含 `7:0-7:10` 瑕疵原样保留）；数据行按模板口径落座：第 i 格装区间 i+1 的值，**末列装次日首区间**（2025-12-31 行置 0），当日 0:00–0:10 无独立列；`全天购电量/购电费` 仍为当日 144 区间的真实合计。充放电量区块表与紧急购电量明细表**不变**（区块按钟点对齐，紧急明细格式本就符合表 4 范式）。 |
| `results/CUMCM2026_C/result1.xlsx` | 行标签改为模板原样 `0:10-0:20 … 0:00+1-0:10+1`，数据循环移位：第 r 行装区间 r+1 的值，末行"次日首时段"填典型日首时段值（典型日逐日重复，无信息损失，144 个值一个不丢）。 |
| `results/CUMCM2026_C/result2/3/4-2/4-3.xlsx` | 由已保存的逐时段 JSON（`problem2_plan3_final.json`、`problem3_sp_strict.json`、`problem4_2_sp.json`、`problem4_3_sp.json`）重新导出，无任何重算。 |
| `src/CUMCM2026_C/validate_results.py` | ① 期望表头改为 `plan_purchase_labels_as_template()`；② result1 行标签校验切到 `"template"` 变体；③ "全天购电量/购电费复算"检查按模板口径重写公式：真合计 = 格子和 − 本行末列 + 前行末列（本行末列是次日首区间、前行末列是本日首区间），费用按区间对齐价格；首行（2/1）的区间 1 藏在 1/31 行、不在表内，首行豁免。该检查此前留有"价格循环移位"的半截注释未实现，本次补全。 |
| `docs/` | 版本对照与修正记录新增 2026-09-13 裁定条（含对旧 R8/A4 decision 口径的废止）；统一分析结论的旧输出约定处加废止指针。 |
| `paper/` | 问题1 框架 §8 写作红线更新为模板口径；问题2/3/4 框架的口径声明清单各增一条结果文件口径声明。 |

## 三、影响范围

- **数值：零变化（已核验）**。四份宽表的"全天购电量"合计逐位不变（如 result2 = 22,373,483.11 kWh）；result1 数值多重集一致；论文表 1/表 2/表 3 的全部成品数字不变。
- **抽查一致性**：替换后按标签查数正确——标签 `14:00-14:10` 的列装的确实是 14:00–14:10 时段的值（与 JSON 逐位核对通过）。
- **19 张论文图（`pictures/`）：零影响**。绘图脚本只读 `problem*_sp.json` 与附件原始数据，不读结果工作簿；曲线、散点、分布图原样成立，无需重画。
- **验证器**：切换后 67 项通过、0 失败、0 缺失。
- **复现链**：`export_results.export_scenario` 现默认输出模板口径；重导出命令不变（从 JSON 调 export 即可）。

## 四、数值零变化与影响范围的成因

"数值零变化、影响面止于导出层"不是巧合，是本项目分层架构的直接结果：

1. **计算与呈现解耦，JSON 是唯一事实来源**。整条管线分为计算层（SARIMA 预测 → 随机规划 → 执行仿真 → 结算）与呈现层（xlsx 导出）。计算层的全部逐时段结果（购电、充放电、SOC、紧急、电价、费用）都以 JSON 落盘（`problem*_sp.json`），xlsx 只是这份事实数据的一种"视图"。本次改动只动了视图层——JSON 一个字节未动，所以一切由 JSON 派生的数字（论文表 1/2/3、图片曲线、全天合计）不可能变化。
2. **本次改动在数学上是一次"置换 + 改名"**。每个时段的值本身不变，变的只是两件事：它落在哪个单元格（数据位 i 从装区间 i 改为装区间 i+1，是一次平移置换；result1 是循环置换），以及每个格子顶上的标签字符串。置换不改变数值，也不改变任何按区间含义进行的查询结果——这正是切换到模板口径的目的（按标签查数反而更准了，如 `14:00-14:10` 列现在装的就是 14:00–14:10 的值）。
3. **全天合计与落座无关**。"全天购电量/购电费"在导出时直接由 144 维向量求和/点积得出，先算后放，与格子座次无关，因此逐位不变。
4. **图片天然免疫**。`make_paper_figures.py` 只读 JSON 与附件原始数据，从不读结果工作簿——呈现层内部（xlsx 之间）的任何变化都传不到图上。这是当初有意的设计选择，本次正好兑现了它的价值。
5. **真正变化的三样东西**：表头字符串、单元格座次、以及验证器里"从格子反推合计"的一致性公式（必须把置换关系写进公式，格子和与真合计之间的换算才会重新闭合——这就是那条复算检查重写的原因，也是它此前"半截注释"想表达而未实现的语义）。
6. **充放电量与紧急购电两表不变的原因**：它们的面板标签/范式本身与 144 列错位无关（4 小时区块按钟点对齐，紧急明细按"起-止"区间自描述），不受模板左移瑕疵波及，因此座次与数值双重不变。

## 五、论文写作注意事项

1. **按标签取数，不要按列序号数格子**。正文引用具体时段数值时，从论文表 1/2/3 的成品数字或 `data/processed/CUMCM2026_C/*.json` 按时段含义取；若直接翻结果文件，必须按表头字符串定位列。
2. **表注必须声明**（各问框架的口径声明清单已含此条）：结果表时段列为官方模板原样口径——第 i 列装区间 i+1 的值；当日 0:00–0:10 无独立列、只计入全天购电量/购电费汇总；宽表末列为次日首区间（12/31 置 0）；result1 末行为次日首时段 = 典型日首时段值；`7:0-7:10` 为模板笔误、原样保留。
3. **由第 2 条产生的两个可解释现象**（评审若追问）：某行 144 个格子的直接求和 ≠ 全天购电量（差 = 次日首区间 − 当日首区间，量级约 ±1,500 kWh）；充放电量 4 小时区块与宽表列不按同一座次对齐（区块按钟点、宽表按模板错位），两者是模板固有语义差异，不是数据错误。
4. **首行豁免说明**：2/1 行的当日首区间值不在本表内（在 1/31 行末列），验证器对该行的合计复算豁免；论文如引用 2/1 的分时段数据，从 JSON 取。
5. 旧口径（decision，物理标签 0:00 起）自 2026-09-13 起废止；历史文档中的相关描述以版本对照记录的裁定条为准。
6. **附件时间戳 = 时段终点**（与本次改动无关、但常被一并追问的口径）：附件1/2/4 中标 `0:10` 的列表示 0:00–0:10 时段，故 0:10–0:20 时段的购电按 `0:20` 列价格结算。依据：终点戳下 144 列恰铺满 [0:00,24:00)（起点戳会丢失当天 0:00–0:10 且每日末列漏入次日）；题面"0:00+1 表示第二天凌晨 0:00，即当天 24:00"的说明只与终点戳相容；官方模板的区间串正是终点戳被误拼为区间的产物；电价/负载/光伏三套数据同戳，起点读法将使每日首时段负载光伏无数据。两种配对对费用的影响实测仅 0.1 元/日量级（`tools/test_time_alignment.py`），但语义上以终点戳为准。**价格-时段配对与单元格落座是两个独立的口径**：前者自始未变（计算层固定"区间 t × 第 t 列"），后者才是本次置换的对象。另须区分**记账语义与信息结构**：时间戳只规定哪列价格配哪个时段（记账），不规定该价格何时可知——问题1–3 电价为日前已知的固定关税表（题面"若每天的电价相同"），0:00 全天价格在握；问题4 的价格未知性由因果裁定处理（计划用预测、结算按交易时刻实际价、日内修正只用已实现价格），全程无"用未来价格做当下决策"。
